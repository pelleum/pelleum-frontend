import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

const createPostScreen = () => {
    return (
        <View>
            <Text>Create a post or thesis</Text>
        </View>
    );
};

export default createPostScreen;

const styles = StyleSheet.create({});
